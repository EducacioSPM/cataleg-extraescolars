name: Test repository dispatch

on:
  repository_dispatch:
    types: [update-catalog]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Mostrar missatge
        run: |
          echo "EVENT REBUT DES DE POWER AUTOMATE"
          echo "Tot funciona"
