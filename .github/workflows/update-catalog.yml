name: Test repository dispatch

on:
  repository_dispatch:
    types: [update-catalog]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Mostrar payload
        run: |
          echo '${{ toJson(github.event.client_payload) }}'
